{% extends 'base.html.twig' %}

{% block title %}Sneaker{% endblock %}

{% block body %}
    <h1>Sneaker</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sneaker.id }}</td>
            </tr>
            <tr>
                <th>Brand</th>
                <td>{{ sneaker.brand }}</td>
            </tr>
            <tr>
                <th>Model</th>
                <td>{{ sneaker.model }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ sneaker.type }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ sneaker.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_sneaker_index') }}">back to list</a>

    <a href="{{ path('app_sneaker_edit', {'id': sneaker.id}) }}">edit</a>

    {{ include('sneaker/_delete_form.html.twig') }}
{% endblock %}
